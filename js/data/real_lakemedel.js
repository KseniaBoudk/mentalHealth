"use strict";

/* Generated by prototype/pipeline/build_kurvan_data.py. Do not hand-edit.
   One file per source (see this script's own module docstring for why) —
   this one is just REAL_LAKEMEDEL_MH. Regenerate with:
     python prototype/pipeline/fetch_socialstyrelsen_mh.py
     python prototype/pipeline/fetch_socialstyrelsen_psych.py
     python prototype/pipeline/build_kurvan_data.py

   Empty `rows` means this source's fetcher has not been run on this
   machine yet — js/data.js falls back to the labelled-synthetic
   generator for that indicator only, rather than showing nothing or
   fabricating something under a "real data" label. */

/* Real, region-grain rates for psychiatric medication dispensed, all nine
   of Kurvan's age bands and all three sexes, annual, split into five
   medication-class series (antidepressants, ADHD medication,
   antipsychotics, anxiety medication, sleep medication) plus a
   synthesised "all" total. Fetched by fetch_socialstyrelsen_lakemedel.py.
   Was assumed to need a multi-gigabyte bulk download (the microdata
   register does) — this is a separate, small aggregate table on the same
   API as the other real Socialstyrelsen indicators here. Rows are compact
   tuples, not objects — see this module's own docstring ("COMPACT TUPLE
   ROWS"). */
const REAL_LAKEMEDEL_MH = {"generated_at":"2026-08-26T08:09:06+00:00","source":"Socialstyrelsen Statistikdatabasen (Läkemedelsregistret, ATC N05/N06)","rows":[]};
